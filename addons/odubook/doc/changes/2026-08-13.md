# 2026-08-13 — Documentation Book

## `user_guide.md`

### Added

- Described the **By date** / **By module** switch, unread entries in bold, the
  green **New** label, **Mark all as read** and the three-month unread horizon.

## `admin_guide.md`

### Added

- Documented where read marks are stored, who may touch them, and the
  `NEW_ENTRY_DAYS` / `UNREAD_HORIZON_DAYS` constants.

## `doc/i18n/pl`, `doc/i18n/ru`

### Added

- Change entries are now mirrored per file at `doc/i18n/<lang>/changes/`, and
  the Book serves them in the reader's language with a per-entry fallback to
  the English source.

### Changed

- Re-translated both guides from the new source revision and refreshed the SHA
  markers.

## `tech_spec.md`

### Added

- Specified the `odubook.change.read` model, its ACL and record rule, the new
  `/odubook/change` and `/odubook/changes/read_all` endpoints, and the unread
  and New rules.
- Recorded the application icon of the root `Book` menu: the built-in
  `fa fa-book,#FFFFFF,#00A09D` form instead of the default Odoo app image.

### Changed

```diff
- get_changes() returns rendered days with entry HTML
+ get_changes() returns a flat index; read_changes() renders and marks as read
```
