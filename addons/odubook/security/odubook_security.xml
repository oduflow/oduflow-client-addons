<odoo>
    <!-- Отметки прочтения строго персональные: чужие строки не видны и не пишутся. -->
    <record id="odubook_change_read_own_rule" model="ir.rule">
        <field name="name">Own read marks only</field>
        <field name="model_id" ref="model_odubook_change_read"/>
        <field name="domain_force">[('user_id', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('base.group_user'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>
</odoo>
