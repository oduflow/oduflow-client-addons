# MCP settings moved to users

- Removed the separate User Access records and menu.
- Added **MCP Active** and **Profile** to the existing **Account Security**
  tab on the user form.
- Removed stored request, failure, and last-use counters; quotas now rely on the
  immutable audit log.
- Existing assignments and event subscriptions are preserved during upgrade.
