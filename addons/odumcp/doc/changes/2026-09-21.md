# Unified platform and client MCP

- Version `16.0.1.4.0` brings wildcard business-method policies, framework/private-method guards, direct approval links, seeded profiles, empty-field-list semantics and policy-controlled system parameters from the platform.
- Preserves managed-key idempotence, superuser enforcement, row locking, existing user policy and audit source attribution. Both previous managed key names are reconciled on provisioning, and both caller response formats remain supported.
- Keeps `deploy/odumcp_server` and its tests, packaging and publishing resources.
- Upgrade existing `odumcp` installations in place. Review empty readable/writable field lists before upgrading: they now allow all otherwise eligible fields for that operation.
