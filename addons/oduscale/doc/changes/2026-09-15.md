# Oduscale Documentation

- Added user instructions for granting employee VPN access, enrolling devices,
  inspecting synchronization results and revoking keys, devices or all access.
- Added administrator documentation for installation, permissions, Headscale
  configuration, secret handling and recovery from service errors.
- Published Polish and Russian guides and this change entry for the Book.
- Moved module-specific instructions out of the repository overview and linked
  the deployment runbook and technical specification from the module documentation.
- Corrected server field labels to API URL, Login URL and Odoo URL.
- Moved reusable Oduflow service deployment instructions into the administrator guide; archived demo installation details in the deployment journal and added agent recording rules.
- Moved Oduscale service configuration into `addons/oduscale/deploy/` and the shared deployment journal into root `log/`; updated links and agent recording rules.
