# Start Chat opens the new conversation reliably

Starting a chat from the agent list (the Start Chat button) no longer
shows a "Missing Record" error: Discuss now always opens the channel of
the newly created conversation instead of reusing the record id from the
list row.
