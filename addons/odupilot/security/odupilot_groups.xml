<odoo>
    <record id="group_bridge" model="res.groups">
        <field name="name">OduPilot Bridge Service</field>
        <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
        <field name="comment">Technical group for the AI chat bridge service account. It unlocks the bridge RPC endpoints, which read the whole command outbox and hand out deployment secrets. Assign it to the bridge account only, never to a person.</field>
    <field name="category_id" ref="base.module_category_hidden"/></record>
</odoo>
